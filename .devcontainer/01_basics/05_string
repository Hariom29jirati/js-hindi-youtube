let str = 'hariom'
console.log(str)

let name = "hariom"
let age = "20"
// console.log("my name is " + name + " and age is " + age)  this is ola way 

console.log(`my name is ${name} and age is ${age}`)
const newString = new String("Hariom   is one of the greatest")
console.log(newString.__proto__)
console.log(newString.length)
console.log(str.__proto__)
console.log(str.length)

console.log(newString.slice(0, 3))
console.log(newString.trim())
console.log(newString.charAt(2))
console.log(newString.indexOf('i'))
console.log(newString.toUpperCase())
